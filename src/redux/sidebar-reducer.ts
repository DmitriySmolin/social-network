import { SidebarType, ActionsTypes } from "./redux-store";

let initialState = {};

export const sidebarReducer = (
  state: SidebarType = initialState,

  action: ActionsTypes
) => {
  return state;
};
